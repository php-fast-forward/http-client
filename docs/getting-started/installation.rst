Installation
============

To install the FastForward HTTP Client, use Composer:

.. code-block:: bash

   composer require fast-forward/http-client

Requirements:
-------------
- PHP ^8.3
- fast-forward/container (for autowiring)

The package will automatically register the HTTP client services when used with the FastForward container system.